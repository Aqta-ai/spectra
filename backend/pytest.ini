[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    benchmark: performance benchmark (deselect with -m 'not benchmark')
    integration: integration test (deselect with -m 'not integration')
    slow: slow test (deselect with -m 'not slow')
    live: live API test (requires USE_LIVE_API=1 and valid GOOGLE_API_KEY)
