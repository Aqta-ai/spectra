export const metadata = {
  title: "Spectra Demo | Voice-Controlled Screen Reader",
  description:
    "Watch the Spectra demo — the best voice-controlled screen reader. Hands-free browsing with AI vision.",
};

export default function DemoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
