export const metadata = {
  title: "Spectra Demo (Dev) | With Controls",
  description: "Interactive demo with playback controls for development",
};

export default function DemoDevLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
